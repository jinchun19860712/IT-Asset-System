# IT 资产管理系统 — 操作指南

本指南面向**系统管理员与日常使用人员**，介绍如何操作系统功能。部署 / 安装 / 启动请见 [DEPLOY.md](./DEPLOY.md)。

---

## 1. 登录与账号

- 打开前端地址（默认 `http://localhost:5173/`），进入登录页。
- 默认账号：`admin` / `admin123`（首次启动自动创建，仅当库中无用户时）。
- 会话有效期 **7 天**（Cookie `itam_session`），过期需重新登录。
- 角色分两级：
  - **管理员（admin）**：可管理字典、产品类型、自定义字段、用户等全部功能。
  - **普通用户**：可使用资产录入、查看等常规操作，部分管理接口返回 403。
- 🔐 **安全提醒**：默认密码仅用于初始化，请尽快在账号设置中修改；设备登录密码、SNMP 团体字在库中为明文，注意数据库文件安全。

---

## 2. 界面布局

系统采用左侧**双树 + 右侧主工作区**布局：

- **组织架构树 / 资产分类树**（左侧切换）：按「部门 / 组织」或「资产类别」两种方式浏览设备。
- **主工作区**：随菜单切换仪表盘、设备列表、字典、合同、告警等视图。
- 常用入口：仪表盘、设备台账、字典管理、产品类型、自定义字段、软件、SNMP 配置、合同、告警、审计日志、拓扑、机柜。

---

## 3. 设备台账管理

设备是系统的核心对象，对应视图 **设备列表（DeviceList）/ 设备表单（DeviceForm）**。

### 3.1 新增 / 编辑设备
- 点击「新增设备」打开表单，填写名称、资产编号、品牌、型号、IP、SN、所属部门 / 分类等。
- **产品类型 + 自定义字段**：选择产品类型后，表单会按该产品类型绑定的自定义字段**动态渲染**（支持文本框、数字、日期、下拉等 11 种字段类型）。
- **端口细分**：若设备定义了端口类型，表单按端口类型分别填写数量，系统汇总为 `port_count`。
- 保存即写入 `devices` 表。

### 3.2 双树归类
- 设备在「组织架构树」下归属部门，在「资产分类树」下归属资产类别；两者独立维护（`folders` 表 `kind ∈ {org, asset}`）。
- 文件夹支持新增 / 重命名 / 删除；若路径异常，可在文件夹管理界面点击「重建路径」自愈。

### 3.3 批量操作
- 在设备列表勾选多条记录，可执行**批量删除 / 批量更新**（对应后端 `/devices/bulk-delete`、`/devices/bulk-update`，单事务执行）。

---

## 4. 字典管理

视图 **字典管理（DictManager）**，维护四类基础字典（`dictionaries` 表 `type`）：
- `product_type` 产品类型
- `brand` 品牌
- `supplier` 供应商
- `software_category` 软件分类

字典项供设备表单下拉选择，建议先在此维护好再录入设备。对应接口：`/dictionaries/types`、`/dictionaries/all`、`/dictionaries/{type}`。

---

## 5. 产品类型与自定义字段

- **产品类型管理（ProductTypeManager）**：定义产品类型，并绑定该类型需要的自定义字段。
- **自定义字段管理（CustomFieldManager）**：维护字段定义（字段名、类型、是否必填等）。
- 当某产品类型绑定了自定义字段，新增该类设备时表单自动出现对应字段，数据库按需加列（`db_migrate` 自愈）。

---

## 6. 软件管理

视图 **软件列表（SoftwareList）/ 软件表单（SoftwareForm）**：维护软件台账（名称、版本、许可证、关联设备等），支持批量删除 / 更新。

---

## 7. SNMP 监控

视图 **配置页（ConfigPage）/ SNMP 相关**。系统对设备采集指标并动态生成 SNMP 列（数据存 `SnmpMetricValue` 表）。

### 7.1 默认模拟模式
- 采集配置 `backend/app/config/snmp_config.json` 中 `"simulate": true` 时，轮询值返回**随机模拟数据**，便于无真实设备时演示与开发。
- OID 模板在 `backend/app/config/oid_config.yaml` 中定义。

### 7.2 切换真实采集
1. 安装 `pysnmp`（`requirements.txt` 已含）。
2. 将 `snmp_config.json` 的 `"simulate"` 改为 `false`，并填写 `default_version`（如 `v2c`）、`default_port`（161）、`default_community`（如 `public`）。
3. 在设备表单填写设备的 SNMP 团体字 / 版本 / 端口。
4. 触发采集；真实采集失败时会自动降级为模拟，保证页面不崩。

> 注：真实采集依赖网络可达与设备 SNMP 开启；生产环境建议使用 SNMPv3 并妥善保管凭据。

---

## 8. 导入导出（Excel）

相关逻辑在 `backend/app/routers/import_export.py`，对应接口：
- `GET  /devices/import-template` 下载空白导入模板
- `POST /devices/import-preview` 上传 `.xlsx` 仅解析与差异分析，**不写库**
- `POST /devices/import` 正式导入，**单事务，失败整体回滚**
- `GET  /devices/export` 导出，含三个 Sheet

### 8.1 导入流程
1. 在设备列表点击「下载模板」，获取含填写说明的 Excel（三个 Sheet：设备台账 38 列 / 端口 13 列 / 机柜 7 列）。
2. 按表头填写数据，保存。
3. 点击「导入」→ 先走「预览」校验（重复 / 字段错误会提示），确认无误后「正式导入」。
4. 导入失败会自动回滚，不会留下半截数据。

### 8.2 导出
- 点击「导出」，生成包含设备、端口、机柜的完整 Excel，用于备份或离线盘点。

---

## 9. 合同附件

视图 **合同列表（ContractList）**：为设备关联采购 / 维保合同。
- 上传合同文件（落盘于 `backend/data/uploads/contracts/`）。
- 支持查看、下载、编辑、删除（对应 `/contracts` 路由族）。

---

## 10. 告警与审计

- **告警列表（AlertList）**：展示 SNMP 采集触发的告警。
- **审计日志（AuditLogList）**：记录关键操作（增删改、导入等），便于追溯。未登录访问受保护接口会返回 401，无权限返回 403。

---

## 11. 拓扑与机柜

- **拓扑视图（TopologyView）**：基于设备 IP / 连接关系可视化网络拓扑。
- **机柜视图（RackView）**：按机柜管理设备物理位置。

---

## 12. 常见问题

**Q：登录提示 401 / 跳回登录页？**
A：会话过期或未登录。清理浏览器 Cookie 重新登录；确认账号为 `admin/admin123`（首次启动自动建）。

**Q：SNMP 一直显示随机数据？**
A：处于模拟模式。将 `backend/app/config/snmp_config.json` 的 `simulate` 改为 `false` 并配置设备 SNMP 信息，见第 7 节。

**Q：设备表单缺少我想要的字段？**
A：去「自定义字段管理」新增字段，并在「产品类型管理」中将其绑定到对应产品类型。

**Q：导入报错但不知原因？**
A：先用「预览」功能，系统会列出每一行的差异与错误，按提示修正后再正式导入。

**Q：数据库文件在哪？会不会被提交到 Git？**
A：位于 `backend/data/it_asset.db`，已在 `.gitignore` 中忽略，不会误提交。请勿手工外传含真实凭据的数据库。

---

> 本文档与 [DEPLOY.md](./DEPLOY.md)、[README.md](./README.md) 配套使用。
